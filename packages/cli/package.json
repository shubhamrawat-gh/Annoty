{
  "name": "annoty",
  "version": "1.1.0",
  "description": "100% on-device visual inspector, style diff engine, and context compiler CLI",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "annotation",
    "prompt-engineering",
    "prompt-generation",
    "cursor",
    "claude",
    "chatgpt",
    "developer-tools",
    "cli"
  ],
  "files": [
    "dist"
  ],
  "bin": {
    "annoty": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && node -e \"const fs = require('fs'); const path = require('path'); const src = path.resolve('..', 'overlay', 'dist', 'overlay.js'); const dest = path.resolve('dist', 'overlay.js'); if (fs.existsSync(src)) { fs.copyFileSync(src, dest); console.log('Successfully copied overlay.js to dist'); } else { console.log('Warning: overlay.js not found in overlay/dist'); }\""
  },
  "dependencies": {
    "commander": "^12.0.0",
    "open": "^10.1.0",
    "picocolors": "^1.0.0",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/prompts": "^2.4.9",
    "typescript": "^5.3.3"
  }
}
